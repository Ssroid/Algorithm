#include <stdio.h>
#include <string.h>

int main(void){
	char name[10000];
	gets(name);
	int j = strlen(name);
	int i =0;
	for(i =0; i<j;i++) {
		if(65<=name[i] && name[i]<=90) {
			printf("%c", name[i]);
		}
	}
	return 0;
}
